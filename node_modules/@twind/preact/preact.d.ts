import { Options } from 'preact';
import { Token, CSSRules, Falsy, TW, Configuration } from 'twind';
export * from 'twind';

interface TwindPreactConfiguration {
    props?: {
        tw?: boolean;
        css?: boolean;
        className?: boolean;
    };
}
declare module 'preact' {
    namespace JSX {
        interface DOMAttributes<Target extends EventTarget> {
            tw?: Token;
            css?: CSSRules | Falsy;
            class?: string;
            className?: string;
        }
    }
}
declare const setup: ({ props, ...config }?: (TwindPreactConfiguration & {
    tw: TW;
}) | (TwindPreactConfiguration & Configuration), options?: Options) => void;

export { TwindPreactConfiguration, setup };
//# sourceMappingURL=preact.d.ts.map
